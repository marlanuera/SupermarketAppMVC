<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Shopping Cart - SunnySide Market</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">

  <style>
    body { background-color: #FFFBEA; font-family: Arial, sans-serif; }
    nav.navbar { background-color: #2E8B57 !important; }
    nav .navbar-brand { font-family: 'Pacifico', cursive; font-size: 1.8rem; color: white !important; }
    nav .nav-link { color: white !important; font-weight: bold; }
    nav .nav-link:hover { background-color: #FFD54F; color: black !important; border-radius: 5px; }

    .cart-table img { max-width: 100px; height: auto; }
    .btn-delete { background-color: transparent; border: none; color: #FF8C00; font-size: 1.2rem; }
    .btn-delete:hover { color: #FFA500; }

    .order-summary { background-color: #FFF5E1; padding: 20px; border-radius: 10px; margin-top: 30px; }
    .order-summary h4 { color: #2E8B57; margin-bottom: 15px; }

    .btn-orange { background-color: #FFA500; border: none; color: white; font-weight: bold; }
    .btn-orange:hover { background-color: #FFB84D; }

    .btn-clear { background-color: #FF4C4C; border: none; color: white; font-weight: bold; }
    .btn-clear:hover { background-color: #FF1A1A; }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mt-4">
    <h2>Shopping Cart</h2>

    <% if (!cart || cart.length === 0) { %>
      <p>Your cart is empty. <a href="/shopping">Go shopping</a>.</p>
    <% } else { %>
      <table class="table table-bordered cart-table align-middle text-center mt-3">
        <thead>
          <tr>
            <th>Product</th>
            <th>Category</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Total</th>
            <th>Remove</th>
          </tr>
        </thead>
        <tbody>
          <% let subtotal = 0; %>
          <% cart.forEach(item => { %>
            <tr>
              <td>
                <img src="/images/<%= item.image %>" alt="<%= item.productName %>" class="img-thumbnail mb-2"><br>
                <strong><%= item.productName %></strong>
              </td>
              <td><%= item.category %></td>
              <td>$<%= item.price.toFixed(2) %></td>
              <td>
                <form action="/update-cart/<%= item.cartId %>" method="POST">
                  <input type="number" name="quantity" value="<%= item.quantity %>" min="1" class="form-control" onchange="this.form.submit()">
                </form>
              </td>
              <td>$<%= (item.price * item.quantity).toFixed(2) %></td>
              <td>
                <form action="/remove-from-cart/<%= item.cartId %>" method="POST">
                  <button type="submit" class="btn-delete"><i class="bi bi-trash-fill"></i></button>
                </form>
              </td>
            </tr>
            <% subtotal += item.price * item.quantity; %>
          <% }) %>
        </tbody>
      </table>

      <div class="order-summary">
        <h4>Order Summary</h4>
        <p>Subtotal (<%= cart.length %> items): $<%= subtotal.toFixed(2) %></p>
        <p>Tax (9%): $<%= (subtotal * 0.09).toFixed(2) %></p>
        <p><strong>Total: $<%= (subtotal * 1.09).toFixed(2) %></strong></p>

        <div class="d-flex justify-content-between mt-4 flex-wrap">
          <a href="/shopping" class="btn btn-warning mb-2">Continue Shopping</a>

          <form action="/cart/clear" method="POST" class="mb-2">
            <button type="submit" class="btn btn-clear">Clear All</button>
          </form>

          <a href="/checkout" class="btn btn-orange mb-2">Proceed to Checkout</a>
        </div>

        <ul class="mt-3">
          <li>✓ Free delivery on orders over $50</li>
          <li>✓ Easy returns within 7 days</li>
        </ul>
      </div>
    <% } %>
  </div>
</body>
</html>
