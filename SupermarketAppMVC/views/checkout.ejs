<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Checkout - SunnySide Market</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">

  <style>
    body { background-color: #FFFBEA; font-family: Arial, sans-serif; }
    nav.navbar { background-color: #2E8B57 !important; }
    nav .navbar-brand { font-family: 'Pacifico', cursive; font-size: 1.8rem; color: white !important; }
    nav .nav-link { color: white !important; font-weight: bold; }
    nav .nav-link:hover { background-color: #FFD54F; color: black !important; border-radius: 5px; }
    .checkout-container { margin: 30px auto; max-width: 900px; }
    .checkout-header h2 { color: #2E8B57; margin-bottom: 20px; }
    .order-summary { background-color: #FFF5E1; padding: 20px; border-radius: 10px; margin-top: 20px; }
    .order-summary h4 { color: #2E8B57; margin-bottom: 15px; }
    .btn-orange { background-color: #FFA500; border: none; color: white; font-weight: bold; }
    .btn-orange:hover { background-color: #FFB84D; }
    .btn-secondary { background-color: #6c757d; border: none; color: white; font-weight: bold; }
    .btn-secondary:hover { background-color: #5a6268; }
    .cart-table img { max-width: 100px; height: auto; }
    .btn-container { display: flex; justify-content: space-between; margin-top: 15px; flex-wrap: wrap; gap: 10px; }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">SunnySide Market</a>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/shopping">Continue Shopping</a></li>
          <li class="nav-item"><a class="nav-link" href="/cart">View Cart</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container checkout-container">
    <div class="checkout-header">
      <h2>Checkout</h2>
    </div>

    <% if (!cart || cart.length === 0) { %>
      <p>Your cart is empty. <a href="/shopping">Go shopping</a>.</p>
    <% } else { %>

      <table class="table table-bordered cart-table align-middle text-center">
        <thead>
          <tr>
            <th>Product</th>
            <th>Category</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Total</th>
          </tr>
        </thead>
        <tbody>
          <% cart.forEach(item => { %>
            <tr>
              <td>
                <img src="/images/<%= item.image %>" alt="<%= item.productName %>" class="img-thumbnail mb-2"><br>
                <strong><%= item.productName %></strong>
              </td>
              <td><%= item.category %></td>
              <td>$<%= item.price.toFixed(2) %></td>
              <td><%= item.quantity %></td>
              <td>$<%= (item.price * item.quantity).toFixed(2) %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <div class="order-summary">
        <h4>Order Summary</h4>
        <p>Subtotal (<%= cart.length %> items): $<%= subtotal.toFixed(2) %></p>
        <p>Tax (8%): $<%= tax.toFixed(2) %></p>
        <p><strong>Total: $<%= total.toFixed(2) %></strong></p>

        <div class="btn-container">
          <a href="/cart" class="btn btn-secondary">Back to Cart</a>

          <!-- Use a form to submit the checkout POST request -->
          <form action="/checkout" method="POST" style="flex: 1;">
            <button type="submit" class="btn btn-orange w-100">Proceed to Payment</button>
          </form>
        </div>

        <ul class="mt-3">
          <li>✓ Free delivery on orders over $50</li>
          <li>✓ Easy returns within 7 days</li>
        </ul>
      </div>
    <% } %>
  </div>
</body>
</html>



